#!/bin/bash

mariadb-dump --host=${MYSQL_HOST} --skip-ssl --password=`cat ${MYSQL_ROOT_PASSWORD_FILE}` --single-transaction --ignore-table-data=${MYSQL_DATABASE}.log_entries ${MYSQL_DATABASE} > /backup/backup_`date +%Y-%m-%d-%H-%M`_hourly.sql
